import React from 'react';
import { Button } from 'antd';

const LogoutButton = () => {
    return (
        <Button
            onClick={() => {console.log('logout not yet implemented')}}
        >
            Logout
        </Button>
    );
}

export default LogoutButton;